#pragma once

// #include "LocalPoint.h"

template<typename T>
class Operator
{
public:
  using value_t = T;
  // using index_t = LocalPoint2d;

  // class OperatorEngine
  // {
  // public:
  //   using value_t = T;
  //   using index_t = LocalPoint2d;
  //   virtual ~OperatorEngine() {}
  //   virtual value_t operator()(index_t const &) const = 0;
  // };
};
